# Choosing your stack

Start from what your agent needs. Whatever the branch, the same rule holds: **demo/paper first, opt in to real money explicitly, cap every write.**

```mermaid
flowchart TD
  Q{"Agent needs?"} --> A["interactive tools<br/>→ MCP server / skill"]
  Q --> B["strategy code<br/>→ SDK / CLI"]
  Q --> C["test before real keys<br/>→ paper trading"]
```

[← back to README](../README.md)
